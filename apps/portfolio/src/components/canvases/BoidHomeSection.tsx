"use client";
import { useEffect, useState } from "react";
import { BoidCanvas } from "./BoidCanvas";

export function BoidHomeSection({
  className,
  wrapperClass,
}: {
  wrapperClass?: string;
  className?: string;
}) {

  return (
    <div className={wrapperClass}>
    </div>
  );
}
